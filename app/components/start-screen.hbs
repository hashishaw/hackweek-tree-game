<div class='start-outer'>
  <div class='scene'>
    <div class='weather-box'>
      {{#if this.showButton}}
        <div class='grow' {{did-insert this.grow}}>
        </div>
      {{/if}}
      <div class='fade-in'>
        <div class='seed-fall'>
          <div class='seed'>
          </div>
        </div>
      </div>
    </div>
    <div class='ground-box'></div>
    <div class='intro'>
      <div class='intro-box'>
        The first thing you remember is darkness. Even still, you reached out a
        tendrill and eventually found light, and water, and everything you
        needed to grow. One year later, this is the beginning of your story.
      </div>
      {{#if this.showButton}}
        <div class='button-outer'>
          <button
            {{did-insert this.fadeIn}}
            class='start-button title'
            {{on 'click' @startGame}}
          >Start Game</button>
        </div>
      {{/if}}
    </div>
  </div>
</div>

{{yield}}